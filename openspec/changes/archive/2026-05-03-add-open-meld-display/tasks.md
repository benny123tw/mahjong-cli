## 1. Open-meld rendering core

- [x] 1.1 In `internal/play/play_test.go`, add a failing test `TestRenderHandIncludesOpenMeldsWithSeatMarkers` that plants three open melds via `g.SetTestMeld` (`MeldPon{5p, From: SeatEast}`, `MeldChi{2m3m4m, From: SeatWest}`, `MeldKan{KanAnkan, 1z×4}`) on the human seat, plus a 13-tile concealed hand; renders the layout via `m.renderHand()`; asserts the output contains `[E]` followed by three `5p` glyphs, `[W]` followed by `2m 3m 4m` glyphs, and `[A]` followed by four `1z` glyphs, per the Open Meld Display For Human Player requirement.
- [x] 1.2 In `internal/play/play_test.go`, add a failing test `TestRenderHandPreservesByteIdenticalOutputWhenNoMelds` that plants a 13-tile concealed-only hand (no melds, no `SetTestMeld` calls) and asserts the rendered output of `m.renderHand()` after the change exactly equals the same Model's `renderHand()` output without the open-meld extension active. The simplest check: assert the rendered string does NOT contain any `[E]`/`[S]`/`[W]`/`[N]`/`[A]` substring when the meld list is empty.
- [x] 1.3 In `internal/play/play.go`, add a `func (m Model) renderOpenMelds() string` helper that walks `m.game.Melds(HumanSeat)` and returns a single horizontally-joined block: for each meld, emit a seat-source marker followed by the meld's tiles, with single-tile-width gaps between adjacent melds. The marker is `[E]`/`[S]`/`[W]`/`[N]` for `MeldPon`/`MeldChi`/`MeldKan{KanMinkan|KanShouminkan}` (read `From Seat`), and `[A]` for `MeldKan{KanAnkan}`. The helper returns "" when the meld list is empty. Use `lipgloss.JoinHorizontal` to build the block; render each tile via `m.renderer.Tile(t)` so Unicode and ASCII renderers stay consistent.
- [x] 1.4 In `internal/play/play.go`, extend `renderHand` to compute the open-meld block via `renderOpenMelds()` and append it to the right of the concealed-hand block via `lipgloss.JoinHorizontal` when the block is non-empty, separated from the concealed row by 2 tile-widths of whitespace. When the meld block is empty, return the existing concealed-only output unchanged (regression-safety per the Open Meld Display For Human Player requirement). Per the Open Meld Display For Human Player requirement.

## 2. Wrap handling for wide hands

- [x] 2.1 In `internal/play/play_test.go`, add a failing test `TestRenderHandWrapsMeldsWhenRowExceeds80Columns` that plants 4 ankans + 13 concealed tiles (the worst-case wide hand) and asserts the rendered output contains a newline character separating the concealed-hand row from the open-meld block, AND each row's visual width does not exceed 80 columns. Use `lipgloss.Width(line)` to measure each line.
- [x] 2.2 In `internal/play/play.go`, before joining the open-meld block to the right of the concealed-hand block, compute the total render width: `concealedWidth + 2*Width() + meldsWidth`. When the total exceeds 80 columns (the existing `targetWidth` constant), `renderHand` SHALL stack the meld block on a SECOND line below the concealed-hand row via `lipgloss.JoinVertical(lipgloss.Top, concealedLine, meldsLine)` instead of `lipgloss.JoinHorizontal`. Per the Open Meld Display For Human Player requirement.

## 3. Verification

- [x] 3.1 Run `go test ./...` from the repository root and confirm all packages pass — especially that no existing play-package render test broke from the new meld-display extension.
- [x] 3.2 Run `golangci-lint run ./...` and resolve any lint issues introduced by the new helper or extension.
- [x] 3.3 Build the binary (`go build -o /tmp/mahjong-cli .`) and confirm a smoke run still launches: `/tmp/mahjong-cli play --seed 42` starts cleanly without panic. Stop the run with `q` after the deal renders.
