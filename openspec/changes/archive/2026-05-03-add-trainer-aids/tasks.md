## 1. Machi peek `?` toggle

- [x] 1.1 In `internal/play/play_test.go`, add a failing test `TestQuestionMarkTogglesPeekVisibility` that constructs a Model via `NewWithGame` (or planted fixture), asserts `m.peekVisible == false` initially, dispatches `tea.KeyPressMsg{Code: '?'}`, asserts the returned Model has `peekVisible == true`, dispatches the key again, asserts `peekVisible == false`. Per the Trainer Aids In The Action Footer requirement.
- [x] 1.2 In `internal/play/play.go`, add a `peekVisible bool` field on `Model` (default zero/false). Per the Trainer Aids In The Action Footer requirement.
- [x] 1.3 In `internal/play/play.go`, add a `'?'` case in the `tea.KeyPressMsg` switch in `Update` that flips `m.peekVisible = !m.peekVisible` and returns the model unchanged otherwise (no game step, no ack text). Per the Trainer Aids In The Action Footer requirement.
- [x] 1.4 In `internal/play/keys.go`, change the `?` Peek entry in `FooterKeys` from `Greyed: true` to `Greyed: false` so the binding renders live. Per the Trainer Aids In The Action Footer requirement.
- [x] 1.5 In `internal/play/play.go`, at the three existing peek-cache reset sites (`handleDiscard` after a successful step, the bot-tick handler that clears `peekShanten`, and the pending-transition setter), set `m.peekVisible = false` so the peek auto-hides on state change. Per the Trainer Aids In The Action Footer requirement.

## 2. Wait-line footer rendering

- [x] 2.1 In `internal/play/play_test.go`, add a failing test `TestRenderFooterShowsWaitLineWhenPeekVisibleAndTenpai` that plants a 13-tile tenpai hand `1m2m3m4p5p6p7s8s9s2z2z3z3z` (waits on 4p/7p) via `SetTestHand`, calls `m.refreshPeek()` (or whatever populates the cache — invoke via the path in `Init` if needed) so `peekShanten==0` and `peekMachi` matches, sets `m.peekVisible = true`, and asserts the output of `m.renderFooter()` (or `m.View().Content` filtered to the footer line) contains the substring `Wait: 4p 7p`. Per the Trainer Aids In The Action Footer requirement.
- [x] 2.2 In `internal/play/play_test.go`, add a failing test `TestRenderFooterShowsNotTenpaiWhenPeekVisibleAndNotTenpai` that plants a non-tenpai hand, sets `peekVisible = true`, asserts the rendered footer contains the substring `Wait: (not tenpai)`. Per the Trainer Aids In The Action Footer requirement.
- [x] 2.3 In `internal/play/play.go`, extend `renderFooter()` so that when `m.peekVisible == true`, the function appends a newline followed by either `Wait: <id1> <id2> ...` (when `peekShanten == 0` and `len(peekMachi) > 0`, joining `tile.Tile{ID: id}.String()` of each entry with single spaces) or `Wait: (not tenpai)` (otherwise). The wait line SHALL come AFTER the existing keys row and AFTER the optional ack-text suffix. Per the Trainer Aids In The Action Footer requirement.

## 3. Furiten badge rendering during human's turn

- [x] 3.1 In `internal/play/play_test.go`, add a failing test `TestFuritenBadgeAppearsWhenHumanTenpaiAndFuriten` that plants a tenpai hand for `SeatSouth`, plants a discard of one of the human's machi tiles in the human's pond via `SetTestPond` (so `IsFuriten` returns true), sets state to `StateAwaitingDiscard{Player: SeatSouth}`, asserts the rendered footer (Unicode renderer path) contains the substring `[FURITEN]`. Per the Trainer Aids In The Action Footer requirement.
- [x] 3.2 In `internal/play/play_test.go`, add a failing test `TestFuritenBadgeASCIIUsesParenForm` that uses the same fixture as 3.1 but with `ASCIIRenderer{}`, asserts the rendered footer contains `(furiten)` and does NOT contain `[FURITEN]`. Per the Trainer Aids In The Action Footer requirement.
- [x] 3.3 In `internal/play/play_test.go`, add a failing test `TestFuritenBadgeAbsentWhenNotTenpai` that uses a non-tenpai hand with the same furiten-trigger pond (which `IsFuriten` already short-circuits to false for non-tenpai per the existing implementation) and asserts the rendered footer does NOT contain the badge. Per the Trainer Aids In The Action Footer requirement.
- [x] 3.4 In `internal/play/play_test.go`, add a failing test `TestFuritenBadgeAbsentInCallWindow` that plants `StateAwaitingClaims` with the human in furiten + tenpai and asserts the rendered footer does NOT add a separate `[FURITEN]` badge (the call window's `[R]on (furiten)` Ron-button suffix is the only furiten indicator there per the existing Call Window Prompt requirement). Per the Trainer Aids In The Action Footer requirement.
- [x] 3.5 In `internal/play/play.go`, add a `furitenBadgeStyle` lipgloss style (red foreground) near the other style definitions. In `renderFooter()`, add a branch that — only when the current state is `StateAwaitingDraw{Player: HumanSeat}` or `StateAwaitingDiscard{Player: HumanSeat}` AND `hand.Shanten(hand.Hand{Concealed: m.game.Hand(HumanSeat)}) == 0` AND `m.game.IsFuriten(HumanSeat)` is true — appends the badge to the footer. The Unicode path renders the badge through `furitenBadgeStyle`; the `ASCIIRenderer` path emits the literal `(furiten)` string with no styling. Detect renderer mode via `_, isASCII := m.renderer.(ASCIIRenderer)` (or the equivalent type-assertion already used elsewhere in the file). Per the Trainer Aids In The Action Footer requirement.

## 4. Verification

- [x] 4.1 Run `go test ./...` from the repository root and confirm all packages pass — especially that no existing play package test broke from the new `peekVisible` field default or the `renderFooter()` extensions.
- [x] 4.2 Run `golangci-lint run ./...` and resolve any lint issues introduced by the new field, key handler, or renderFooter branches.
- [x] 4.3 Build the binary (`go build -o /tmp/mahjong-cli .`) and confirm a smoke run still launches: `/tmp/mahjong-cli play --seed 42` starts cleanly without panic. Stop the run with `q` after the deal renders.
