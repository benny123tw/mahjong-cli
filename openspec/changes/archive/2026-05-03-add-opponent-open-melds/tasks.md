## 1. Extract zone-width constants

- [x] 1.1 In `internal/play/play.go`, extract the existing hard-coded zone widths from `renderMidRow` into named constants: `kamichaZoneWidth = 20`, `shimochaZoneWidth = 20`, `toimenZoneWidth = 80` (or wire it to `targetWidth`). Replace the magic numbers in `lipgloss.NewStyle().Width(20)` calls with these constants. No behavior change — purely a rename for use in tasks 2 and 3.

## 2. Add `renderOpponentMelds` with wrap and truncation

- [x] 2.1 In `internal/play/play_test.go`, add a failing test `TestRenderOpponentMeldsZeroMeldCase` that constructs a `play.Model` with a `*game.Game` whose East/North/West seats have ZERO open melds, then asserts `m.renderOpponentMelds(SeatEast, kamichaZoneWidth) == ""`. Per the zero-meld scenario in the Play Screen Layout requirement.
- [x] 2.2 In `internal/play/play_test.go`, add a failing test `TestRenderOpponentMeldsFitsOnOneLine` that plants a single pon of 5p from South onto SeatEast, then asserts `m.renderOpponentMelds(SeatEast, kamichaZoneWidth)` returns a single-line string (no `\n`) whose `lipgloss.Width` is ≤ `kamichaZoneWidth` AND contains the `5p` glyph and the `[S]` marker. Per the one-line scenario in the Play Screen Layout requirement.
- [x] 2.3 In `internal/play/play_test.go`, add a failing test `TestRenderOpponentMeldsWrapsWhenTooWide` that plants TWO pons on SeatEast (e.g., pon of 5p from South, pon of 1m from West) so the combined block exceeds 20 cols. Assert `m.renderOpponentMelds(SeatEast, kamichaZoneWidth)` returns a multi-line string with exactly 2 lines (one meld per line) AND each line's `lipgloss.Width` is ≤ `kamichaZoneWidth`. Per the wrap scenario in the Play Screen Layout requirement.
- [x] 2.4 In `internal/play/play_test.go`, add a failing test `TestRenderOpponentMeldsTruncatesWithKMore` that plants 4 ankans on SeatEast (the worst case described in the spec example table). Assert `m.renderOpponentMelds(SeatEast, kamichaZoneWidth)` returns a 3-line string where lines 1-2 contain the first N ankans whose cumulative width fits in `2 * kamichaZoneWidth`, AND line 3 contains the substring `+K more` where K = `4 - N`. Per the truncation scenario in the Play Screen Layout requirement.
- [x] 2.5 In `internal/play/play.go`, implement `renderOpponentMelds(seat game.Seat, zoneWidth int) string`. The function SHALL: (1) call `renderOpenMeldsForSeat(seat)` to get the raw single-line meld block; (2) early-return "" when the raw block is "" (zero-meld case); (3) early-return the raw block if `lipgloss.Width(rawBlock) <= zoneWidth`; (4) otherwise, walk the per-meld blocks (one per call to `renderOpenMeldsForSeat` with a single-meld slice — OR refactor `renderOpenMeldsForSeat` to expose a per-meld slice helper) and use a greedy fitter: for each meld, place it on a line where adding it does not exceed `zoneWidth`. If the meld would push the current line past `zoneWidth`, start a new line. Cap at 2 lines. (5) If after 2 lines there are still unrendered melds, render the partially-filled 2-line block, then append `+K more` on a 3rd line via `lipgloss.JoinVertical`. K is the count of unrendered melds. Use `labelStyle` for the `+K more` suffix. Per the Play Screen Layout requirement and the renderEndPanel layout decisions.

## 3. Wire opponent meld rendering into the three opponent-zone renderers

- [x] 3.1 In `internal/play/play_test.go`, add a failing parameterized test `TestOpponentZonesIncludeMelds` that loops over the three opponent seats `{SeatEast → "Kamicha · East", SeatNorth → "Toimen — North", SeatWest → "Shimocha · West"}`. For each seat, plant a single pon (5p from South for SeatEast, 1m from West for SeatNorth, 9p from East for SeatWest), render the full View via `m.View().Content`, and assert: (a) the rendered output contains the meld glyph and the seat-source marker, (b) the meld block appears AFTER the seat label substring, (c) the meld block appears BEFORE the seat's discard-pond first tile (or the back-row's first tile in the Toimen case). Position assertions use `strings.Index` to compare offsets. Per the opponent-zone rendering requirement in the Play Screen Layout.
- [x] 3.2 In `internal/play/play.go`, modify `renderKamichaColumn()`, `renderShimochaColumn()`, and `renderToimenRow()` to insert the opponent meld block between the seat label and the zone's other content. Use `renderOpponentMelds(SeatEast, kamichaZoneWidth)`, `renderOpponentMelds(SeatWest, shimochaZoneWidth)`, and `renderOpponentMelds(SeatNorth, toimenZoneWidth)` respectively. The shape for each: `label + ("\n" + melds if melds != "") + ("\n" + remainingContent if remainingContent != "")`. For Toimen specifically, the remaining content is the back-row followed by the pond; the meld block sits between label and back-row. Per the Play Screen Layout requirement.

## 4. Regression check: zero-meld byte-identical rendering

- [x] 4.1 In `internal/play/play_test.go`, add a failing test `TestOpponentZonesUnchangedWithZeroMelds` that constructs a `play.Model` with all three opponents holding zero open melds. Capture the rendered Kamicha column output by calling `m.renderKamichaColumn()` directly (or the equivalent zone-render entry point). Run a SECOND assertion that artificially clears the model's match's per-seat melds (already empty, but assert robustness) and the rendered Kamicha column output equals the first capture exactly (byte-for-byte). Repeat for Shimocha and Toimen. The test SHALL fail if the meld-block insertion adds any whitespace or extra newline in the zero-meld case. Per the zero-meld scenario in the Play Screen Layout requirement.

## 5. Verification

- [x] 5.1 Run `go test ./...` from the repository root and confirm all packages pass — especially the existing layout tests (the `renderToimenRow`, `renderKamichaColumn`, `renderShimochaColumn` regression tests, plus the existing meld-display and end-of-hand-panel tests, are not broken by the new meld insertion).
- [x] 5.2 Run `golangci-lint run ./...` and resolve any lint issues introduced by the new helper, the zone-width constants, or the three renderer modifications.
- [x] 5.3 Build the binary (`go build -o /tmp/mahjong-cli .`) and confirm the demo flag still launches cleanly: `/tmp/mahjong-cli play --demo-end ron` starts and shows the panel without panic. The reveal panel test in this run also exercises the per-seat meld rendering for all four seats, which is the same `renderOpenMeldsForSeat` machinery the opponent zones now use.
