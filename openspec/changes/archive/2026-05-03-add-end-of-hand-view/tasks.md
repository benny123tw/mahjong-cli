## 1. Verify engine surface (no engine changes; reads only)

- [x] 1.1 Read `internal/game/state.go`, `internal/game/turn.go`, and `internal/game/payout.go` to confirm: (a) `StateRoundOver` carries the `Outcome` value, (b) `Outcome` is a sum type with `OutcomeRon{Winner, Loser, Result}`, `OutcomeTsumo{Winner, Result}`, `OutcomeRyuukyoku{TenpaiPlayers}`, (c) `Result` carries the yaku list, han, fu, base score, and winning tile, (d) `ComputePayouts(outcome, ctx) [4]int` is callable from the play package, (e) the play-screen Model already has a `pendingAck` (or equivalent) holding the captured `Outcome` and `IsChankan` flag. If any of (a)-(e) is wrong, pause and reconcile the design with the actual engine surface BEFORE implementing the renderer.

## 2. Generalize `renderOpenMelds` to take a seat parameter

- [x] 2.1 In `internal/play/play_test.go`, add a failing test `TestRenderOpenMeldsForSeatRendersCorrectSeatMelds` that plants distinct open melds on `SeatEast` (a pon of 1m from South) and `SeatNorth` (an ankan of east winds), then asserts `m.renderOpenMeldsForSeat(SeatEast)` contains the 1m glyphs and the seat-source marker, and `m.renderOpenMeldsForSeat(SeatNorth)` contains four EastWind glyphs and `[A]`. The test SHALL fail with "undefined function" until task 2.2 is in place.
- [x] 2.2 In `internal/play/play.go`, rename `renderOpenMelds` to `renderOpenMeldsForSeat(seat game.Seat) string` and replace `m.game.Melds(HumanSeat)` with `m.game.Melds(seat)`. Update the lone existing call site in `renderHand` to call `m.renderOpenMeldsForSeat(HumanSeat)`. Run the existing meld-display tests to confirm they still pass — no behavior change for the human-seat path. Per the End-of-Hand Acknowledgement requirement (the function generalization is required for the four-seat reveal).

## 3. End-of-hand reveal panel: ron win

- [x] 3.1 In `internal/play/play_test.go`, add a failing test `TestEndPanelRonRevealsHandsAndYakuBreakdown`. Setup: construct a `Match` and step it into `StateRoundOver` with `OutcomeRon{Winner: SeatSouth, Loser: SeatEast, Result: ...}` where `Result` carries `YakuList = [{"Riichi", 1}, {"Pinfu", 1}, {"Tanyao", 1}]`, `Han = 3`, `Fu = 30`, `Award.Base = 480`, `WinningTile = {ID: P5}`, deltas = `[East -3900, South +3900, West 0, North 0]`. Plant a known concealed hand on each seat (use `g.SetTestHand` per seat, ensure the South hand contains a 5p). Construct a `play.Model` bound to that match, render via `m.View()` (or the panel-render path the implementation uses), then assert the rendered output contains: `RON` header substring with `South` and `East` named, every seat's concealed-hand glyphs, `Yaku: Riichi 1 · Pinfu 1 · Tanyao 1`, `Han 3 · Fu 30 · Base 480`, `East -3900`, `South +3900`, and `[Any key — Continue]`. Per the End-of-Hand Acknowledgement requirement.
- [x] 3.2 In `internal/play/endpanel.go` (NEW), create the `renderEndPanel(m Model) string` function. Switch on the captured outcome variant: `OutcomeRon` → `renderWinPanel(m, outcome, isChankan)`; other variants are stubbed to return "" for now (handled in tasks 3.3 + 3.4). The win-panel renderer SHALL: (1) emit a header line via `fmt.Sprintf("RON — %s wins on %s from %s", winnerLetter, tileGlyph, loserLetter)`, (2) emit four seat rows by looping seats E/S/W/N, calling `renderOpenMeldsForSeat` for each, prefixing the winner with `[W] ` and bold-styling that seat's row, (3) within the winner's hand row, locate the first index whose tile-ID matches `Result.WinningTile.ID` and render that index with `focusedTileStyle` to apply the bold highlight, (4) emit the spacer line, (5) emit `Yaku: <name1> <han1> · <name2> <han2> · ...` joined from `Result.YakuList`, (6) emit `Han N · Fu M · Base K`, (7) emit the deltas line by formatting `ComputePayouts(outcome, ctx) [4]int` in seat order with sign, (8) emit the `[Any key — Continue]` footer. Wire `renderEndPanel` from the existing ack-panel render branch in `play.go` (replace the minimal-content branch when the outcome is `OutcomeRon`, `OutcomeTsumo`, or `OutcomeRyuukyoku`). Per the End-of-Hand Acknowledgement requirement.
- [x] 3.3 Add a focused test `TestEndPanelRonWinningTileIsHighlighted` that constructs the same ron fixture and asserts: the rendered output contains the bold ANSI escape sequence `\x1b[1m` immediately preceding the `5p` glyph within the South seat's hand row, AND the South seat row begins with `[W] `. Per the winner-row highlight + winning-tile highlight in the End-of-Hand Acknowledgement requirement.
- [x] 3.4 Add a focused test `TestEndPanelRonChankanLabelsHeader` that constructs the same ron fixture but with the pending ack flag `IsChankan = true`, then asserts the rendered header contains `CHANKAN RON` instead of `RON`. Implement the conditional in `renderWinPanel`: when `isChankan` is true, the header substring SHALL read `CHANKAN RON` instead of `RON`. Per the chankan-ron scenario in the End-of-Hand Acknowledgement requirement.

## 4. End-of-hand reveal panel: tsumo win

- [x] 4.1 In `internal/play/play_test.go`, add a failing test `TestEndPanelTsumoRevealsHandsAndYakuBreakdown`. Setup: construct a `Match` and step it into `StateRoundOver` with `OutcomeTsumo{Winner: SeatSouth, Result: ...}` where `Result` carries the same yaku list and totals as task 3.1, `WinningTile = {ID: P5}`, and deltas = `[East -2000, South +4000, West -1000, North -1000]` (non-dealer tsumo). Assert the rendered output contains: `TSUMO` header substring with `South` named (NO `from <seat>` clause for tsumo), every seat's concealed-hand glyphs, the same `Yaku:` and `Han N · Fu M · Base K` lines, the deltas line, and the footer. Per the End-of-Hand Acknowledgement requirement.
- [x] 4.2 In `internal/play/endpanel.go`, extend the variant-switch in `renderEndPanel` to handle `OutcomeTsumo` by calling `renderWinPanel(m, outcome, isChankan: false)` with the tsumo-specific header (`TSUMO — <winner> wins`, no discarder clause). The yaku/han/fu/deltas lines reuse the same code path as ron. Per the End-of-Hand Acknowledgement requirement.

## 5. End-of-hand reveal panel: ryuukyoku

- [x] 5.1 In `internal/play/play_test.go`, add a failing parameterized test `TestEndPanelRyuukyokuLabelsAndPayments` that loops over all 5 rows from the ryuukyoku-payment example table (0/4, 1/3, 2/2, 3/1, 4/0). For each row: construct a `Match` and step it into `StateRoundOver` with `OutcomeRyuukyoku{TenpaiPlayers: <seat list>}`; plant a known concealed hand on each seat (the planted shanten does NOT need to match `TenpaiPlayers` — the panel reads the tenpai set off the outcome, not by re-computing shanten); render the panel; assert the rendered output contains the `RYUUKYOKU` header, every seat's concealed-hand glyphs, the inline tag `tenpai` for each seat in `TenpaiPlayers` and `noten` for the rest, the deltas line matching the row exactly in seat order East/South/West/North with sign, and the `[Any key — Continue]` footer. Per the End-of-Hand Acknowledgement requirement and the ryuukyoku-payment example table.
- [x] 5.2 In `internal/play/endpanel.go`, extend the variant-switch in `renderEndPanel` to handle `OutcomeRyuukyoku`. The ryuukyoku-panel renderer SHALL: (1) emit `RYUUKYOKU` as the header, (2) build a set from `outcome.TenpaiPlayers` (use a `[4]bool` indexed by seat), (3) for each seat row emit `tenpai` or `noten` inline at the end of the row's content, (4) emit the deltas line by formatting `ComputePayouts(outcome, ctx) [4]int` in seat order, (5) emit the `[Any key — Continue]` footer. Per the End-of-Hand Acknowledgement requirement.

## 6. Footer + key-handler integration

- [x] 6.1 TDD the footer swap. First add a failing test `TestEndPanelFooterReplacesActionFooter` in `internal/play/play_test.go` that places the model in `StateRoundOver` with any outcome and asserts the rendered output contains the substring `[Any key — Continue]` AND does NOT contain the normal action footer's substrings (e.g., `Move`, `Discard`, `Riichi`). Then in `internal/play/play.go`, when the ack panel is active (the existing `pendingAck != nil` branch), change the footer-render path to emit `[Any key — Continue]` instead of `RenderActionFooter`. The existing keypress-advances-the-hand handler is preserved untouched. Per the End-of-Hand Acknowledgement requirement.

## 7. Verification

- [x] 7.1 Run `go test ./...` from the repository root and confirm all packages pass — especially the existing `End-of-Hand Acknowledgement` test (the keypress-advance scenario) is not broken by the new render content.
- [x] 7.2 Run `golangci-lint run ./...` and resolve any lint issues introduced by the new helper, the `endpanel.go` file, or the function rename in tasks 2.2.
- [x] 7.3 Build the binary (`go build -o /tmp/mahjong-cli .`) and confirm a smoke run still launches: `/tmp/mahjong-cli play --seed 42` starts cleanly without panic. The smoke run does NOT need to manually drive the game to a hand-end (the goldens cover that); just confirm startup is clean.
