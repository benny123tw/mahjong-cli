## 1. Engine: ShouldRiichi heuristic

- [x] 1.1 Update Bot Decision Strategy: bot riichi heuristic picks the first tenpai-leaving index. In `internal/game/bot.go`, replace the current `Bot.ShouldRiichi` (always false) with a new signature: `func (b Bot) ShouldRiichi(hand []tile.Tile, score int, wallRemaining int, isOpen bool) (declare bool, tileIdx int)`. Implementation: return `(false, 0)` when `len(hand) != 14 || score < 1000 || wallRemaining < 4 || isOpen`. Otherwise iterate `idx := 0..len(hand)-1`, build `postDiscard` by removing `hand[idx]`, call `hand.Shanten(hand.Hand{Concealed: postDiscard})`, return `(true, idx)` on the first index whose shanten is 0. Fallthrough returns `(false, 0)`.
- [x] 1.2 Tests cover both engine-level bot helpers and play-level dispatch: in `internal/game/bot_test.go`, add tests covering `Bot.ShouldRiichi` — tenpai-after-discard returns `(true, idx)`; not-tenpai returns `(false, 0)`; open hand returns `(false, 0)` regardless of tenpai; score<1000 returns `(false, 0)`; wall<4 returns `(false, 0)`. Use `tenpaiHandReady()` (from add-human-agari's riichi_test.go) as the canonical 13-tile shape and append a 14th tile for the discard fixtures.

## 2. Engine: Furiten gate generalized to all seats

- [x] 2.1 Generalize Human Ron From Claim Window — furiten gate applies to all seats — drop the human-only guard. In `internal/game/turn.go`, in `stepFromAwaitingClaims`'s `ClaimRon` branch, change the furiten gate from `if winner == HumanSeat && g.IsFuriten(winner)` to `if g.IsFuriten(winner)`. Drop the human-only guard so bot ron is also blocked by permanent furiten.
- [x] 2.2 In `internal/game/furiten_test.go`, add a test asserting bot ron is rejected with `ErrFuritenRon` when the bot's machi tile is in the bot's own pond. Mirror the existing `TestHumanRonRejectedWhenFuriten` test using a non-human seat (e.g., `SeatNorth`).

## 3. TUI: Bot Action Dispatch — AwaitingDiscard branch (tsumo + riichi + fallback)

- [x] 3.1 Implement Bot Action Dispatch for the `AwaitingDiscard` branch — bot tsumo check runs before the discard decision. In `internal/play/play.go`'s `handleBotTick`, before invoking `bot.PickDiscard`, build the bot's 14-tile `hand.Hand` (with the rightmost tile as `Winning`, `IsTsumo: true`) and run `calc.Analyze` with a context built from the bot's seat wind, round wind, dora indicators, and the engine's per-seat riichi flags (use `m.game.IsHandOpen(seat)` for the `Open` field of `hand.Hand`). If non-nil, submit `InputDeclareTsumo` and skip the rest of the branch.
- [x] 3.2 Add a `Game.Score(seat Seat) int` getter to `internal/game/turn.go` returning `g.scores[seat]`. The TUI needs this to pass the bot's current score into `ShouldRiichi`; the engine's `scores` slice is unexported.
- [x] 3.3 In the same `AwaitingDiscard` branch from task 3.1, after the tsumo check, invoke `bot.ShouldRiichi(m.game.Hand(seat), m.game.Score(seat), m.game.Wall().LiveRemaining(), m.game.IsHandOpen(seat))`. If `declare == true`, submit `InputDiscard{Index: idx, Riichi: true}`.
- [x] 3.4 In the AwaitingDiscard branch's fallback, keep the existing `InputDiscard{Index: bot.PickDiscard(hand)}` behavior unchanged for when neither tsumo nor riichi triggers.
- [x] 3.5 In `internal/play/play_test.go`, add a test for Bot Action Dispatch tsumo: plant `SeatEast` with a 14-tile winning hand, set state to `AwaitingDiscard{Player: SeatEast}`, drive `BotTickMsg`, assert the model lands in `StateRoundOver{Outcome: OutcomeTsumo}`.
- [x] 3.6 In `internal/play/play_test.go`, add a test for Bot Action Dispatch riichi: plant `SeatEast` with a 14-tile hand whose discard index 13 leaves a tenpai 13-tile shape, set state to `AwaitingDiscard{Player: SeatEast}`, drive `BotTickMsg`, assert state becomes `StateAwaitingClaims{Discarder: SeatEast}` AND `Game.riichiDeclared[SeatEast]` is true.

## 4. TUI: Bot Action Dispatch — AwaitingClaims branch (ron + pon + chi)

- [x] 4.1 Implement Bot Action Dispatch for the `AwaitingClaims` branch in `handleBotTick` — bot ron sits in claims branch per-seat, submitted through the existing resolver. Replace the unconditional `InputResolveClaims{Claims: nil}` with per-seat evaluation. Build a `claims := map[game.Seat]game.Claim{}`. For each seat in `[]Seat{SeatEast, SeatSouth, SeatWest, SeatNorth}` skipping `cs.Discarder` AND skipping `HumanSeat` (the human submits their own claim via R/P/C/Space): evaluate ron → pon → chi in that order, record the first match. Submit `InputResolveClaims{Claims: claims}` in a single call.
- [x] 4.2 Bot ron evaluation: build `concealed := append(hand, cs.Discard)`, run `calc.Analyze` on the resulting `hand.Hand{Concealed: concealed, Winning: cs.Discard, IsTsumo: false, Open: m.game.IsHandOpen(seat)}` with a context built like task 3.1's. If non-nil AND `!m.game.IsFuriten(seat)`, set `claims[seat] = Claim{Kind: ClaimRon}` and continue to next seat (skip pon/chi for this seat).
- [x] 4.3 Bot pon evaluation — bot pon/chi probabilistic decisions use the existing PRNG; no new consumption sites. Skip when ron already recorded for this seat. Compute `isYakuhai := isYakuhaiTile(cs.Discard, m.game.RoundWind(), seat.SeatWind())`, compute `shanten := hand.Shanten(hand.Hand{Concealed: m.game.Hand(seat)})`. If `bot.ShouldPon(hand, cs.Discard, isYakuhai, shanten)` returns true, set `claims[seat] = Claim{Kind: ClaimPon}` and continue.
- [x] 4.4 Bot chi evaluation (kamicha only): skip when ron or pon already recorded for this seat OR when `seat != cs.Discarder.Next()` (only kamicha may chi). Call `bot.ShouldChi(hand, cs.Discard, cs.Discarder, seat)` — if it returns a non-empty `[][2]uint8` options slice, set `claims[seat] = Claim{Kind: ClaimChi, ChiTiles: options[0]}`.
- [x] 4.5 In `internal/play/play_test.go`, add a test for Bot Action Dispatch ron: plant `SeatNorth` with a tenpai shape that wins yakufully on the discard, set state to `AwaitingClaims{Discard: <winning tile>, Discarder: SeatEast}`, drive `BotTickMsg`, assert state advances to `StateRoundOver{Outcome: OutcomeRon{Winner: SeatNorth, Loser: SeatEast}}`.
- [x] 4.6 In `internal/play/play_test.go`, add a test for Bot Action Dispatch pon: plant `SeatNorth` with two East-wind tiles, set state to `AwaitingClaims{Discard: EastWind, Discarder: SeatEast}`, drive `BotTickMsg`, assert state advances to `StateAwaitingDiscard{Player: SeatNorth}` (claim resolved as pon).
- [x] 4.7 In `internal/play/play_test.go`, add a test confirming the bot dispatcher does NOT submit a claim on the human's behalf: plant the human in a winning-on-discard position, drive `BotTickMsg` from `AwaitingClaims`, assert state remains `AwaitingClaims{...}` (waiting for human input) and the human's claim was not auto-submitted.

## 5. Verification

- [x] 5.1 Run `go test ./...` and confirm all suites pass.
- [x] 5.2 Run `golangci-lint run ./...` and confirm 0 issues.
- [x] 5.3 Run `spectra validate add-smart-ai` and confirm valid.
- [x] 5.4 Smoke test: rebuild (`go build -o bin/mahjong .`) and play a full round with `--seed 5` (early pon opportunity from earlier scan). Observe at least one bot pon, one bot discard, and verify the game can complete via either tsumo or ron from any seat.
