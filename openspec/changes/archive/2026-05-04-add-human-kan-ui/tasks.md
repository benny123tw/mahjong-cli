## 1. K-key liveness predicate

- [x] 1.1 In `internal/play/play_test.go`, add a failing test `TestHumanKanLegalAcrossStates` that loops over the 5 cases in the K liveness example table (4-of-a-kind no riichi → true; pon-plus-match no riichi → true; 4-of-a-kind in riichi → false; 3-of-a-kind only → false; non-AwaitingDiscard state → false). For each case, set up the model state (hand contents, riichi flag via `m.match` if needed, `SetTestState`), call `humanKanLegal(m)`, and assert the boolean result matches the expected column. The test SHALL fail with "undefined function" until task 1.2 is in place.
- [x] 1.2 In `internal/play/kan_keys.go`, add `func humanKanLegal(m Model) bool` that returns true when the current state is `StateAwaitingDiscard{Player: HumanSeat}` AND the human has not declared riichi AND EITHER `firstAnkanID(hand)` returns ok=true OR `firstShouminkanID(hand, melds)` returns ok=true. Returns false otherwise (any other state, riichi declared, or no eligible declaration). The function takes the Model and reads `m.game` to get state, hand, melds, and riichi flag. Per the K-liveness predicate in the Keybinding Map requirement.

## 2. Action footer K renders live when humanKanLegal returns true

- [x] 2.1 In `internal/play/play_test.go`, add a failing test `TestActionFooterKLiveWhenAnkanEligible`. Setup: a model in `StateAwaitingDiscard{Player: HumanSeat}` with the human's hand containing 4× P5 plus 10 fillers, no riichi. Render the action footer (call `m.View().Content` and locate the footer line, OR call `renderFooter` directly). Assert the rendered footer styles the `K Kan` substring with `liveKeyStyle` rather than `greyedKeyStyle` (substring check on the SGR sequence — `liveKeyStyle` uses foreground 250, `greyedKeyStyle` uses foreground 240; the test checks the SGR escape that wraps `K Kan`). Per the K-live action-footer scenario in the Keybinding Map requirement.
- [x] 2.2 In `internal/play/play_test.go`, add a failing test `TestActionFooterKGreyedWhenIneligible`. Setup: same model but with only 3× P5 in hand (no ankan eligibility) and no matching open pon (no shouminkan eligibility). Assert the rendered footer styles `K Kan` with `greyedKeyStyle`. Per the K-greyed scenario in the Keybinding Map requirement.
- [x] 2.3 In `internal/play/play.go`, modify the action-footer render path (currently the `for _, b := range FooterKeys` loop in `renderFooter`) so that when the binding is the K key, the live/greyed style is computed from `humanKanLegal(m)` rather than the static `b.Greyed` field. Other bindings remain on the static `b.Greyed` path. Per the K-liveness predicate in the Keybinding Map requirement.

## 3. Kan handler tests

- [x] 3.1 In `internal/play/play_test.go`, add a failing test `TestHandleKanDeclaresAnkan`. Setup: model in `StateAwaitingDiscard{Player: HumanSeat}` with 4× P5 plus 10 fillers (legal ankan). Call `m.handleKan()`. Assert `m.ackText == "ankan declared"` AND the underlying game state is no longer `StateAwaitingDiscard{Player: HumanSeat}` (the engine moves to AwaitingDraw or AwaitingDiscard with a different tile after the rinshan flow — anything except the original state shape proves the kan fired). Per the K-declares-ankan scenario in the Keybinding Map requirement.
- [x] 3.2 In `internal/play/play_test.go`, add a failing test `TestHandleKanDeclaresShouminkan`. Setup: model in `StateAwaitingDiscard{Player: HumanSeat}`, hand contains a 1m, plant an open `MeldPon{Tiles: [1m, 1m, 1m], From: SeatEast}` on the human via `SetTestMeld`. Call `m.handleKan()`. Assert `m.ackText == "shouminkan declared"` AND `m.game.State()` is `StateAwaitingChankan{Declarer: HumanSeat, UpgradeTile: 1m}`. Per the K-declares-shouminkan scenario in the Keybinding Map requirement.
- [x] 3.3 In `internal/play/play_test.go`, add a failing test `TestHandleKanDeclaresMinkan`. Setup: model in `StateAwaitingClaims{Discard: 5p, Discarder: SeatEast}` with 3× P5 in the human's hand (legal minkan). Call `m.handleKan()`. Assert the resulting state is no longer `StateAwaitingClaims` (the minkan resolved and the engine moved on). The exact post-state may be `StateAwaitingDiscard{Player: HumanSeat}` after the rinshan flow — assert that the engine transitioned out of `AwaitingClaims` and that the human now has an open `MeldKan` matching the discard. Per the K-submits-minkan scenario in the Keybinding Map requirement.
- [x] 3.4 In `internal/play/play_test.go`, add a failing test `TestHandleKanGreyedDuringRiichi`. Setup: model in `StateAwaitingDiscard{Player: HumanSeat}` with 4× P5 plus 10 fillers, but the human has declared riichi (set the riichi flag via the engine's test helper, e.g., the existing path used by `TestKanKeyGreyedDuringRiichi`-style tests in the engine layer if available, or via `g.SetTestRiichiDeclared(HumanSeat, true)` if that helper exists). Call `m.handleKan()`. Assert `m.ackText == "kan: not allowed during riichi"` AND the underlying state is unchanged (`StateAwaitingDiscard{Player: HumanSeat}` still active). Per the K-greyed-during-riichi scenario in the Keybinding Map requirement.

## 4. Verification

- [x] 4.1 Run `go test ./...` from the repository root and confirm all packages pass — especially the existing kan engine tests (`internal/game/kan_test.go`) and any meld-display tests are not broken by the new K-liveness wiring.
- [x] 4.2 Run `golangci-lint run ./...` and resolve any lint issues introduced by the new helper, the action-footer change, or the new tests.
- [x] 4.3 Build the binary (`go build -o /tmp/mahjong-cli .`) and confirm the play subcommand still launches cleanly.
